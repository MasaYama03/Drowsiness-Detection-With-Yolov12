[phases.setup]
aptPkgs = ["libglib2.0-0"]

[phases.install]
cmds = [
    ". /opt/venv/bin/activate && pip install -r requirements.txt && pip uninstall -y opencv-python && pip install opencv-python-headless==4.8.1.78"
]
